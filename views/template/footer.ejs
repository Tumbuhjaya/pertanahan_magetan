    <footer style="background-color: #f7f7f7;padding: 60px 0;">
        <div class="container">
            <div class="row">
                <div class="col-lg-4 col-xl-4">
                    <div class="row">
                        <div class="col-lg-12 col-xl-12">
                            <div class="w-100-flex">
                                <div style="width: 25%;display: flex;justify-content: center;align-items: center;background-color: ;">
                                    <img src="/images/template/logo-kota-smg.png" alt="" class="w-100">
                                </div>

                                <div style="width: 75%;display: flex;justify-content: center;align-items: flex-start;flex-direction: column;background-color: ;padding-left: 10px;">
                                    <h6>PEMERINTAH KOTA SEMARANG</h6>
                                    <h5><strong>DINAS PERUMAHAN DAN KAWASAN PERMUKIMAN</strong></h5>
                                </div>
                            </div>
                        </div>
                        <div class="col-lg-12 col-xl-12">
                            <h6 class="mt-2">Jl. Pemuda Nomor 148 Gedung B, Semarang, Jawa Tengah, 50132</h6>
                        </div>
                    </div>
                </div>

                <div class="col-lg-8 col-xl-8">
                    <div class="row">
                        <div class="col-lg-4 col-xl-4">
                            <h5><strong>Rumah Susun Sewa Sederhana</strong></h5>
                            <hr>
                            <a href="/hunian/detail/1" style="color: black;"><h6><i class="fas fa-chevron-right"></i> Bandarharjo</h6></a>
                            <a href="/hunian/detail/4" style="color: black;"><h6><i class="fas fa-chevron-right"></i> Kaligawe</h6></a>
                            <a href="/hunian/detail/6" style="color: black;"><h6><i class="fas fa-chevron-right"></i> Karangroto Baru</h6></a>
                            <a href="/hunian/detail/7" style="color: black;"><h6><i class="fas fa-chevron-right"></i> Karangroto C Lama</h6></a>
                            <a href="/hunian/detail/8" style="color: black;"><h6><i class="fas fa-chevron-right"></i> Kudu</h6></a>
                            <a href="/hunian/detail/9" style="color: black;"><h6><i class="fas fa-chevron-right"></i> Pekunden</h6></a>
                            <a href="/hunian/detail/10" style="color: black;"><h6><i class="fas fa-chevron-right"></i> Plamongan Sari</h6></a>
                            <a href="/hunian/detail/12" style="color: black;"><h6><i class="fas fa-chevron-right"></i> Sawah Besar</h6></a>
                        </div>

                        <div class="col-lg-4 col-xl-4">
                            <h5><strong>Rumah Susun Sewa Pekerja</strong></h5>
                            <hr>
                            <a href="/hunian/detail/3" style="color: black;"><h6><i class="fas fa-chevron-right"></i> Jerakah</h6></a>
                            <a href="/hunian/detail/11" style="color: black;"><h6><i class="fas fa-chevron-right"></i> Pondok Boro</h6></a>
                        </div>

                        <div class="col-lg-4 col-xl-4">
                            <h5><strong>Rumah Deret</strong></h5>
                            <hr>
                            <a href="/hunian/detail/2" style="color: black;"><h6><i class="fas fa-chevron-right"></i> Gasemsari</h6></a>
                            <a href="/hunian/detail/5" style="color: black;"><h6><i class="fas fa-chevron-right"></i> Karangroto A-B</h6></a>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </footer> 
    
    
    <!-- modal login -->
    <div class="modal fade" id="modalLogin" tabindex="-1" aria-labelledby="exampleModalLabel" aria-hidden="true">
        <div class="modal-dialog modal-dialog-centered">
            <div class="modal-content">
                <div class="modal-header">
                    <h5 class="modal-title" id="exampleModalLabel">Log In</h5>
                    <button type="button" class="close" data-dismiss="modal" aria-label="Close">
                    <span aria-hidden="true">&times;</span>
                    </button>
                </div>
                <div class="modal-body">
                    <div class="row">
                        <div class="col-lg-12 col-xl-12">
                            <form method="post" action="/autentifikasi/login_user" >
                                <div class="form-group row">
                                    <label for="" class="col-sm-3 col-form-label">Username</label>
                                    <div class="col-sm-9">
                                    <input type="number" class="form-control" name="username"  placeholder="Silakan masukkan nomor KK anda">
                                    </div>
                                </div>
                                <div class="form-group row">
                                    <label for="" class="col-sm-3 col-form-label">Password</label>
                                    <div class="col-sm-9">
                                    <input type="password" class="form-control" name="password"  placeholder="Silahkan masukkan NIK anda">
                                    </div>
                                </div>

                                <div class="form-group row">
                                    <label for="" class="col-sm-3 col-form-label">Captcha</label>
                                    <div class="col-sm-9">
                                        <div style="width: 100%;border:2px dashed #6777ef;padding: 5px;margin-top: 15px;border-radius: 0.25rem; ;" id="captcha">

                                        </div>
                                        <input type="text" class="form-control"  name="captcha" placeholder="Silahkan masukkan hasil penjumlahan">
                                    </div>
                                </div>
                                
                                <div class="form-group row">
                                    <div class="col-sm-9 offset-3">
                                        <input type="hidden" name="tujuan" value="/dashboard_penghuni/profil">
                                        <input type="hidden" name="target" value="penghuni">
                                        <!-- <button type="submit" class="btn btn-primary">Sign in</button> -->
                                         <button type="submit" class="btn btn-primary">Log In</button>
                                    </div>
                                </div>
                            </form>
                        </div>
                    </div>
                </div>
            <!-- <div class="modal-footer">
                <button type="button" class="btn btn-secondary" data-dismiss="modal">Close</button>
                <button type="button" class="btn btn-primary">Save changes</button>
            </div> -->
            </div>
        </div>
    </div>  
<script>

    async function get_cap(params) {
        let data = await $.get(`/get_captcha`)
        $('#captcha').append(data.data)
        
    }
    get_cap()
</script>
  <script type="text/javascript">
    // $('.tgl1').datepicker({
    //     format: 'yyyy-mm-dd',
    //     orientation: "auto top",
    //     autoclose: true,
    //     todayHighlight: true,
    //   });

  function point_format(a) {
    return a.toString().replace(/(\d)(?=(\d{3})+(?!\d))/g, '$1.')
  }
  var rooturl = ''
  let text_delete = {title:'Berhasil',text:'Data berhasil dihapus',icon:'success',type:'success'}
  let text_insert = {title:'Berhasil',text:'Data berhasil ditambahkan',icon:'success', type:'success'}
  let text_edit = {title:'Berhasil',text:'Data berhasil diedit',icon:'success', type:'success'}
  let text_error = {title:'Gagal',text:'Data gagal ditambahkan',icon:'error', type:'error'}
  
  // Validasi file menggunakan JavaScript
  const MAX_FILE_SIZE = 500 * 1024 ;
  function cek_file(file) {
    // Periksa apakah file ada
    const validType = file.type === "image/jpeg";

    if (!file) {
      return "Silakan pilih file terlebih dahulu.";
    }else

    // Validasi ukuran file
    if (file.size > MAX_FILE_SIZE) {
      return "Ukuran file melebihi 500KB.";
    }else

    // Validasi tipe file
    if (!validType) {
      return "Hanya file .jpg yang diperbolehkan."
    }else

    // Reset pesan kesalahan jika lolos validasi
    {return ""}
  }
   function renderPagination(totalData, currentPage, maxPages , itemsPerPage , ubah_table , tabs) {
        let tab = tabs?tabs:''
      const pagination = document.querySelector('#pagination'+tab);
      pagination.innerHTML = '';
    
      const totalPages = Math.ceil(totalData / itemsPerPage);
    
      function addPageButton(pageNumber) {
        const link = document.createElement('button');
        link.textContent = pageNumber;
        link.onclick = function () {
            ubah_table(pageNumber)
        } 
        if (pageNumber === currentPage) {
          link.classList.add('active');
        }
    
        link.addEventListener('click', function () {
          currentPage = pageNumber;
          renderPagination(totalData, currentPage, maxPages,itemsPerPage,ubah_table, tabs );
        });
    
        pagination.appendChild(link);
      }
    
      function addEllipsis() {
        const ellipsis = document.createElement('button');
        ellipsis.textContent = '...';
        pagination.appendChild(ellipsis);
      }
    
      if (currentPage > 1) {
        const firstButton = document.createElement('button');
        
        firstButton.textContent = 'First';
        firstButton.onclick = function () {
            ubah_table(1)
        } 
        firstButton.addEventListener('click', function () {
          currentPage = 1;
          renderPagination(totalData, currentPage, maxPages,itemsPerPage,ubah_table, tabs );
        });
    
        pagination.appendChild(firstButton);
      }
    
      if (currentPage > 1) {
        const prevButton = document.createElement('button');
    
        prevButton.textContent = 'Previous';
    
        prevButton.addEventListener('click', function () {
          currentPage -= 1;
          renderPagination(totalData, currentPage, maxPages,itemsPerPage,ubah_table, tabs );
        });
        prevButton.onclick = function () {
            ubah_table(currentPage)
        } 
        pagination.appendChild(prevButton);
      }
    
      for (let i = 1; i <= totalPages; i++) {
        if (i === currentPage || i === 1 || i === totalPages || (i >= currentPage - maxPages && i <= currentPage + maxPages)) {
          addPageButton(i);
        } else if (i === currentPage - maxPages - 1 || i === currentPage + maxPages + 1) {
          addEllipsis();
        }
      }
    
      if (currentPage < totalPages) {
        const nextButton = document.createElement('button');
    
        nextButton.textContent = 'Next';
    
        nextButton.addEventListener('click', function () {
          currentPage += 1;
          renderPagination(totalData, currentPage, maxPages,itemsPerPage,ubah_table, tabs );
        });
        nextButton.onclick = function () {
            ubah_table( currentPage)
        } 
        pagination.appendChild(nextButton);
      }
    
      if (currentPage < totalPages) {
        const lastButton = document.createElement('button');
    
        lastButton.textContent = 'Last';
        lastButton.onclick = function () {
            ubah_table(totalPages)
        } 
        lastButton.addEventListener('click', function () {
          currentPage = totalPages;
          renderPagination(totalData, currentPage, maxPages,itemsPerPage,ubah_table, tabs );
        });
    
        pagination.appendChild(lastButton);
      }
    }
   function make_date(date,tipe) {
    const dateObject = new Date(date);

    const day = String(dateObject.getUTCDate()).padStart(2, '0');
    const month = String(dateObject.getUTCMonth() + 1).padStart(2, '0'); // getUTCMonth() mengembalikan bulan dari 0-11
    const year = dateObject.getUTCFullYear();

    const formattedDate = `${day}-${month}-${year}`;
    const formattedDate2 = `${year}-${month}-${day}`;

    return tipe ==1?formattedDate:formattedDate2
   }
   function formatNumber(num) {
      return num.toString().replace(/(\d)(?=(\d{3})+(?!\d))/g, '$1.')
    }
</script>